{
  "name": "personal-site",
  "description": "Site hosted at <https://dagans.dev>",
  "version": "0.1.0",
  "author": "Dagan",
  "license": "AGPL-3.0",
  "homepage": "https://dagans.dev",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx stylelint css/*.css"
  },

  "devDependencies": {
    "stylelint": "^15.10.1",
    "stylelint-config-recommended": "^12.0.0"
  }
}
